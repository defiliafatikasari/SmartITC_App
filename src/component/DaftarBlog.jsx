import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { hitam } from '../constants/warna';

const DaftarBlog = () => {
  return (
    <View>
      <Text style={{color: hitam}}>DaftarBlog</Text>
    </View>
  )
}

export default DaftarBlog;

const styles = StyleSheet.create({})